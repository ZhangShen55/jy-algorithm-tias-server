# Ascend 910B / CANN 8.1.RC1 verified on aarch64 with Python 3.10.
# Install torch, torch-npu and torchvision from the Ascend-compatible wheel
# source used by the deployment environment, then install TIAS runtime deps.
#
# Verified environment:
#   CANN: 8.1.RC1
#   driver: 25.0.rc1.1
#   torch: 2.4.0
#   torch-npu: 2.4.0
#   torchvision: 0.19.0
#   ultralytics: 8.3.156
#
# Example:
#   conda activate tias-910b
#   python -m pip install torch==2.4.0 torch-npu==2.4.0 torchvision==0.19.0
#   python -m pip install -r tias/requirements.txt

-r requirements.txt
