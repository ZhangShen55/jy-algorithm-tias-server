## 1. 配置与依赖准备

- [x] 1.1 新增 `app/ai_quality/` 模块结构和独立 Worker 启动入口，确保不依赖 FastAPI HTTP 服务启动。
- [x] 1.2 新增 AI 课堂视觉分析配置对象，覆盖 Kafka、MySQL、NFS、临时目录、抽帧间隔、抓拍缩放、任务重试次数、Worker 并发数。
- [x] 1.3 补充运行依赖检查，确认 Kafka 客户端、MySQL 客户端、视频处理库和现有模型依赖可用。
- [x] 1.4 增加启动前检查，验证 NFS 挂载根目录存在且可写。

## 2. Kafka 消费与消息模型

- [x] 2.1 实现 Kafka 消息模型解析，支持 `task_id`、学生视频 URL、教师视频 URL、`student_count` 和可选上下文字段。
- [x] 2.2 实现字段兼容映射和关键字段校验，缺少 `task_id` 或视频 URL 时产生可落库的失败原因。
- [x] 2.3 实现 Kafka Consumer，关闭自动提交，按任务成功或最终失败后手动提交 offset。
- [x] 2.4 实现任务级最多 3 次重试，视频不可用等不可恢复错误最终写失败状态并提交 offset。
- [x] 2.5 将 Worker 默认并发设置为 1，并通过配置允许后续调整。

## 3. 数据库访问与仓储封装

- [x] 3.1 新增 MySQL 连接和事务封装，支持配置化连接 `ai_quality` 数据库。
- [x] 3.2 实现 `lesson_ai_workflow` 写入和更新，覆盖处理中、成功、失败、进度和错误摘要。
- [x] 3.3 实现 `lesson_ai_job` 状态更新，覆盖开始时间、结束时间、处理中、成功、失败。
- [x] 3.4 实现 `lesson_behavior_timeline` 批量写入，并使用表唯一键保证同任务同指标同分钟不重复。
- [x] 3.5 实现 `lesson_snapshot_event` 批量写入，保存相对 `image_url`。
- [x] 3.6 实现 `indicator_score_result` 按 `task_id + indicator_id` UPSERT。
- [x] 3.7 实现同一 `task_id` 重跑前清理旧 `lesson_behavior_timeline`、`lesson_snapshot_event`、`lesson_student_behavior_stat`。

## 4. 视频下载、抽帧与临时文件

- [x] 4.1 实现完整视频 URL 下载到任务临时目录，支持超时、异常提示和临时文件清理。
- [x] 4.2 实现视频实际时长读取，失败时抛出任务级错误。
- [x] 4.3 实现 30 秒窗口中点抽帧，抽帧时间点为 15s、45s、75s 等有效时间点。
- [x] 4.4 分别处理学生视频和教师视频抽帧，并保留 `task_id`、时间戳、分钟号、帧序号等元数据。
- [x] 4.5 增加视频不可用、无有效帧、短视频边界场景测试。

## 5. 帧分析能力复用

- [x] 5.1 实现学生帧分析适配器，复用现有人数、人脸、学生行为检测函数，输出 `present_count`、`face_count`、睡觉人数、玩手机人数等统一结构。
- [x] 5.2 实现教师帧分析适配器，复用教师行为检测和 DirectMHP 头部姿态分析，输出教师主体、行为和头部姿态统一结构。
- [x] 5.3 明确教师头部姿态无效帧规则：无教师主体、无头部姿态、状态失败时不参与 `A6-01` 分母。
- [x] 5.4 增加使用 `tests`、`tests2` 样例图片的分析适配器测试，确认现有模型调用路径可用。

## 6. 抓拍存储

- [x] 6.1 实现抓拍图片缩放，按宽高各 1/4 输出 PNG。
- [x] 6.2 实现相对路径生成规则 `cv/{task_id}/{image_id}.png`。
- [x] 6.3 实现实际写入路径 `{snapshot_mount_root}/cv/{task_id}/{image_id}.png`，并自动创建任务目录。
- [x] 6.4 确保 `lesson_snapshot_event.image_url` 只保存相对路径，不拼接 HTTP 域名或 OSS 根地址。
- [x] 6.5 增加抓拍目录不可写和图片写入失败测试。
- [x] 6.6 将 `lesson_snapshot_event` 调整为核心快照事件表，不再保存所有学生抽帧图片。
- [x] 6.7 增加快照事件配置项，覆盖总上限、抬头高峰 TopK/阈值、阅读专注 TopK/阈值、睡觉人数/占比、玩手机人数/占比、教师预警连续帧数、同类最小间隔。
- [x] 6.8 实现学生抬头高峰快照筛选，按抬头率取最高 N 张后再按阈值过滤。
- [x] 6.9 实现学生阅读专注快照筛选，使用 `Read_W` 检测结果按阅读占比取最高 N 张后再按阈值过滤。
- [x] 6.10 实现学生睡觉快照筛选，满足睡觉人数或睡觉占比阈值时生成候选。
- [x] 6.11 实现学生玩手机快照筛选，新增并写入 `behavior_type=5`。
- [x] 6.12 实现教师教态预警快照筛选，连续配置帧数非面向学生或低头时生成候选。
- [x] 6.13 增加快照候选排序、同类最小间隔去重和每课次总上限控制测试。

## 7. 指标聚合与评分

- [x] 7.1 实现有效学生帧过滤，`present_count=0` 的帧不参与抬头率、前排占位率、后排占位率聚合。
- [x] 7.2 实现应到人数分母选择：优先使用 `student_count > 0`，否则使用本任务检测最高人数。
- [x] 7.3 实现 `E2-01 到课率`，使用有效检测人数中位数 / 应到人数。
- [x] 7.4 实现 `E5-01 平均抬头率`，使用有效帧 `face_count / present_count` 聚合。
- [x] 7.5 实现 `E3-01 前排就座率` 占位指标，使用 `task_id + minute_no + metric_type` 稳定随机生成 20%～30% 比例并取整。
- [x] 7.6 实现 `E4-01 后排就座率` 占位指标，使用与前排不同的 `metric_type` seed，确保重跑稳定。
- [x] 7.7 实现 `A6-01 面向学生占比`，使用教师头部姿态正面且非低头的有效帧占比。
- [x] 7.8 实现指标 ID 查询或配置映射，覆盖 `E2-01`、`E3-01`、`E4-01`、`E5-01`、`A6-01`。
- [x] 7.9 实现指标评分，优先解析 `indicator.score_rule`，不可解析时按比例值 `0～1` 映射到 `0～100` 分。
- [x] 7.10 增加聚合单元测试，覆盖中位数、零人数跳过、应到人数兜底、前后排稳定随机、教师无效帧跳过。

## 8. 单课次编排与状态闭环

- [x] 8.1 实现单课次 Worker 编排流程：校验消息、标记处理中、清理旧结果、下载视频、抽帧、分析、存图、聚合、落库、标记成功。
- [x] 8.2 实现任务失败闭环，任一步骤任务级失败时写 workflow/job 失败状态和错误摘要。
- [x] 8.3 实现任务完成后清理临时文件，不删除已写入 NFS 的抓拍图片。
- [x] 8.4 确保成功和最终失败场景都会触发 Kafka offset 提交。
- [x] 8.5 确保 Worker 进程异常退出时不会提前提交 offset。

## 9. 验证与联调

- [x] 9.1 编写配置示例和本地启动说明，包含 Kafka、MySQL、NFS 挂载目录、并发数和重试次数。
- [x] 9.2 使用 `tests`、`tests2` 中的样例资源验证学生帧、教师帧和头部姿态分析链路。
- [x] 9.3 使用本地 Kafka 投递一条有效课次消息，验证完整链路写入 workflow、job、timeline、snapshot、indicator 结果。
- [x] 9.4 使用同一 `task_id` 重跑，验证旧明细清理、指标 UPSERT 和前后排占位结果稳定。
- [x] 9.5 使用不可用视频 URL 投递任务，验证重试 3 次、失败落库和 offset 提交。
- [x] 9.6 运行现有测试和新增测试，确认不影响现有 HTTP 图片推理接口。
- [x] 9.7 使用全量视频重跑，验证 `lesson_snapshot_event` 数量不再等于所有抽帧数，且只包含核心快照事件。

## 10. 学生异常行为聚合统计

- [x] 10.1 增加 `lesson_student_behavior_stat` 统计配置，支持配置统计开始分钟和最多高峰段数量。
- [x] 10.2 实现学生玩手机和趴桌睡觉聚合统计，固定跳过课程前 3 分钟，`detect_count` 仅累计有效统计窗口内的人次。
- [x] 10.3 实现 `peak_period_desc` 生成，按候选段严重程度排序后最多保留配置段数，并按时间顺序展示。
- [x] 10.4 实现 `lesson_student_behavior_stat` 批量写入和 `task_id + behavior_type` 幂等 UPSERT。
- [x] 10.5 将学生异常行为统计接入 Worker 成功链路，并保持同一 `task_id` 重跑前清理旧统计。
- [x] 10.6 增加单元测试，覆盖统计窗口、候选段排序、最多 5 段、配置加载、仓储写入和 Worker 接入。
- [x] 10.7 使用全量视频重跑，验证 `lesson_student_behavior_stat` 写入真实检测到的学生异常行为统计；本次样例未检测到睡觉，不写空行，睡觉写入逻辑由单元测试覆盖。
